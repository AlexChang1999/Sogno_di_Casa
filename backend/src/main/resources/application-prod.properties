# 正式環境設定（覆蓋 application.properties 的預設值）
# 啟用方式：java -jar sognodicasa.jar --spring.profiles.active=prod

# 禁止 Hibernate 自動修改資料表結構（正式環境只做驗證，不動資料）
spring.jpa.hibernate.ddl-auto=validate

# 關閉 SQL 印出（正式環境不需要 log 每一行 SQL）
spring.jpa.show-sql=false

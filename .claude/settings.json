{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); echo \"$f\" | grep -q '\\.java$' && (cd backend && mvn compile -q 2>&1 | tail -5) || true",
            "timeout": 120,
            "statusMessage": "mvn compile..."
          }
        ]
      }
    ]
  }
}

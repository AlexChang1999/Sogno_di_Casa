# Add CRUD Feature
1. Create/update JPA Entity in model/
2. Create/update DTO in dto/
3. Create/update Repository interface
4. Create/update Service class
5. Create/update Controller with proper @RequestMapping
6. Update SecurityConfig to permit new endpoints
7. Run `mvn compile` to verify
8. Update frontend JS/HTML to consume new API
9. Test endpoint with curl
